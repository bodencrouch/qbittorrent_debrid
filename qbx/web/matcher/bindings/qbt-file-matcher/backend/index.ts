/** Re-export shim so existing imports of Wails bindings keep working. */
export { QBitService, MatcherService } from "../../../src/api/backend";
export type { TorrentFile, DiskFile, MatchInfo } from "../../../src/api/backend";
